/* select#1 */ SELECT
                 `strategy_checking`.`a`.`id`                       AS `id`,
                 `strategy_checking`.`a`.`busi_type_id`             AS `busi_type_id`,
                 `strategy_checking`.`a`.`merchant_code`            AS `merchant_code`,
                 `strategy_checking`.`a`.`account_type`             AS `account_type`,
                 `strategy_checking`.`a`.`account_busi_type_id`     AS `account_busi_type_id`,
                 `strategy_checking`.`a`.`accounting_type`          AS `accounting_type`,
                 `strategy_checking`.`a`.`accounting_no`            AS `accounting_no`,
                 `strategy_checking`.`a`.`busi_order_no`            AS `busi_order_no`,
                 `strategy_checking`.`a`.`trans_order_no`           AS `trans_order_no`,
                 `strategy_checking`.`a`.`amount`                   AS `amount`,
                 `strategy_checking`.`a`.`pay_amount`               AS `pay_amount`,
                 `strategy_checking`.`a`.`fee_amount`               AS `fee_amount`,
                 `strategy_checking`.`a`.`order_time`               AS `order_time`,
                 `strategy_checking`.`a`.`trans_time`               AS `trans_time`,
                 `strategy_checking`.`a`.`cur_id`                   AS `cur_id`,
                 `strategy_checking`.`a`.`remark`                   AS `remark`,
                 `strategy_checking`.`a`.`client_site`              AS `client_site`,
                 `strategy_checking`.`a`.`order_no`                 AS `order_no`,
                 `strategy_checking`.`a`.`ticket_method`            AS `ticket_method`,
                 `strategy_checking`.`a`.`order_source`             AS `order_source`,
                 `strategy_checking`.`a`.`order_status`             AS `order_status`,
                 `strategy_checking`.`a`.`order_create_time`        AS `order_create_time`,
                 `strategy_checking`.`a`.`flight_num`               AS `flight_num`,
                 `strategy_checking`.`a`.`cabin`                    AS `cabin`,
                 `strategy_checking`.`a`.`departure_time`           AS `departure_time`,
                 `strategy_checking`.`a`.`dpt_arr`                  AS `dpt_arr`,
                 `strategy_checking`.`a`.`airline_company`          AS `airline_company`,
                 `strategy_checking`.`a`.`pnr`                      AS `pnr`,
                 `strategy_checking`.`a`.`all_prices`               AS `all_prices`,
                 `strategy_checking`.`a`.`pay_type`                 AS `pay_type`,
                 `strategy_checking`.`a`.`policy_id`                AS `policy_id`,
                 `strategy_checking`.`a`.`order_self_status`        AS `order_self_status`,
                 `strategy_checking`.`a`.`ticket_count`             AS `ticket_count`,
                 `strategy_checking`.`a`.`bill_amount`              AS `bill_amount`,
                 `strategy_checking`.`a`.`profit`                   AS `profit`,
                 `strategy_checking`.`a`.`bill_source`              AS `bill_source`,
                 `strategy_checking`.`a`.`check_time`               AS `check_time`,
                 `strategy_checking`.`a`.`record_type`              AS `record_type`,
                 `strategy_checking`.`a`.`record_status`            AS `record_status`,
                 `strategy_checking`.`a`.`agent_prefix`             AS `agent_prefix`,
                 `strategy_checking`.`a`.`create_time`              AS `create_time`,
                 `strategy_checking`.`a`.`update_time`              AS `update_time`,
                 `strategy_checking`.`a`.`unconfirm_amount`         AS `unconfirm_amount`,
                 `strategy_checking`.`a`.`confirm_status`           AS `confirm_status`,
                 `strategy_checking`.`a`.`comments`                 AS `comments`,
                 `strategy_checking`.`a`.`report_status`            AS `report_status`,
                 `strategy_checking`.`b`.`id`                       AS `id`,
                 `strategy_checking`.`b`.`order_id`                 AS `order_id`,
                 `strategy_checking`.`b`.`order_no`                 AS `order_no`,
                 `strategy_checking`.`b`.`ticket_no`                AS `ticket_no`,
                 `strategy_checking`.`b`.`age_type`                 AS `age_type`,
                 `strategy_checking`.`b`.`passenger_name`           AS `passenger_name`,
                 `strategy_checking`.`b`.`express`                  AS `express`,
                 `strategy_checking`.`b`.`airline_company_name`     AS `airline_company_name`,
                 `strategy_checking`.`b`.`from_ticket_no`           AS `from_ticket_no`,
                 `strategy_checking`.`b`.`to_ticket_no`             AS `to_ticket_no`,
                 `strategy_checking`.`b`.`bill_ticket_no`           AS `bill_ticket_no`,
                 `strategy_checking`.`b`.`tax`                      AS `tax`,
                 `strategy_checking`.`b`.`price`                    AS `price`,
                 `strategy_checking`.`b`.`agent_pnr`                AS `agent_pnr`,
                 `strategy_checking`.`b`.`agency_fee`               AS `agency_fee`,
                 `strategy_checking`.`b`.`paid_amount`              AS `paid_amount`,
                 `strategy_checking`.`b`.`airline_company_order_no` AS `airline_company_order_no`,
                 `strategy_checking`.`b`.`trans_no`                 AS `trans_no`,
                 `strategy_checking`.`b`.`bank`                     AS `bank`,
                 `strategy_checking`.`b`.`ticket_time`              AS `ticket_time`,
                 `strategy_checking`.`b`.`refund_fee`               AS `refund_fee`,
                 `strategy_checking`.`b`.`refund_amount`            AS `refund_amount`,
                 `strategy_checking`.`b`.`refund_cause`             AS `refund_cause`,
                 `strategy_checking`.`b`.`record_type`              AS `record_type`,
                 `strategy_checking`.`b`.`record_status`            AS `record_status`,
                 `strategy_checking`.`b`.`create_time`              AS `create_time`,
                 `strategy_checking`.`b`.`update_time`              AS `update_time`,
                 `strategy_checking`.`b`.`qunar_tax`                AS `qunar_tax`,
                 `strategy_checking`.`b`.`tax_balance`              AS `tax_balance`,
                 `strategy_checking`.`b`.`qunar_price`              AS `qunar_price`,
                 `strategy_checking`.`b`.`price_balance`            AS `price_balance`,
                 `strategy_checking`.`b`.`qunar_refund_fee`         AS `qunar_refund_fee`,
                 `strategy_checking`.`b`.`refund_fee_balance`       AS `refund_fee_balance`
               FROM `strategy_checking`.`zy_ticket_bill` `b`
                 JOIN `strategy_checking`.`zy_transaction_order_profit` `a`
                 JOIN (/* select#2 */ SELECT `strategy_checking`.`zy_transaction_order_profit`.`id` AS `id`
                                      FROM `strategy_checking`.`zy_transaction_order_profit`
                                      WHERE ((`strategy_checking`.`zy_transaction_order_profit`.`record_status` = 1) AND
                                             (`strategy_checking`.`zy_transaction_order_profit`.`record_type` = 2) AND
                                             (`strategy_checking`.`zy_transaction_order_profit`.`agent_prefix` = 'xep'))
                                      LIMIT 30000, 10000) `id_tmp`
               WHERE ((`strategy_checking`.`a`.`id` = `id_tmp`.`id`) AND
                      (`strategy_checking`.`b`.`order_id` = `id_tmp`.`id`))
